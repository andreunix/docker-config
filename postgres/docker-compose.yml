# Use postgres/example user/password credentials
version: '3.9'

services:

  db:
    image: postgres:16.3
    restart: always
    environment:
      POSTGRES_PASSWORD: development

    ports:
      - 5432:5432
